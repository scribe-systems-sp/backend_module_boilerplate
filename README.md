# Scribe backend module boilerplate

## Installation
clone repository, install the dependencies and build.

```sh
git clone https://github.com/scribe-systems-sp/backend_module_boilerplate.git module
cd module
yarn install
npm run build
```
